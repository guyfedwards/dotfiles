#!/bin/bash

path="$HOME/Dropbox/Screenshots/"

fname=$(ls -lt $path | egrep -v 'total|./' | head -n1 | awk '{ s = ""; for (i = 9; i <= NF; i++) s = s $i "\\ "; print s }' | rev | cut -d\\ -f2- | rev)

echo "$path$fname"
