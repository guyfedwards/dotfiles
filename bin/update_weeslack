#!/usr/bin/env bash

curdir=$(pwd)

cd "$HOME/.local/share/weechat/python/autoload" || exit 1

curl -O https://raw.githubusercontent.com/wee-slack/wee-slack/master/wee_slack.py

cd "$curdir" || exit 1
